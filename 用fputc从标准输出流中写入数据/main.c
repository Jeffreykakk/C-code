#include <stdio.h>
#include <stdlib.h>

int main() 
{
	fputc('J',stdout);
	fputc('e',stdout);
	fputc('f',stdout);
	fputc('f',stdout);
	fputc('r',stdout);
	fputc('e',stdout);
	fputc('y',stdout);
	return 0;
}